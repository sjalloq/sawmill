"""Shared fixtures for {{ plugin_name }} plugin tests."""

from __future__ import annotations

from pathlib import Path

import pytest

from {{ module_name }}.plugin import {{ class_name }}


@pytest.fixture
def plugin():
    """Return an instance of the {{ plugin_name }} plugin."""
    return {{ class_name }}()


@pytest.fixture
def sample_log(tmp_path: Path) -> Path:
    """Create a sample log file for testing.

    Replace this content with real {{ plugin_name }} tool output.
    """
    log_file = tmp_path / "sample.log"
    log_file.write_text(
        (Path(__file__).parent.parent / "examples" / "sample.log").read_text()
    )
    return log_file
